# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'proto_twitter' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for proto_twitter
  pod 'Firebase/Core'

  target 'proto_twitterTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'proto_twitterUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
